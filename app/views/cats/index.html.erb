<title> All of the cats </title>

<body>
  <ul>
    <% @cats.each do |cat| %>
      <li>
        <%= cat.name %> : <%= link_to "More info", "/cats/#{cat.id}" %>
      </li>
    <% end %>
  </ul>
</body>
